# rhoSimpleFoam — the ground-up port

The systematic re-port of OpenFOAM's `rhoSimpleFoam`, transcribed from OpenFOAM's own source, host `_cpp`
first, CUDA second. **Nothing here is copied** — not from `gpuRhoSimpleFoam.cu`, not from `simpleFoam`,
not from `legacy/`.

## Why start empty when a working solver already exists

`gpuRhoSimpleFoam.cu` runs, and it has four registered gates. It was also built the way that produces the
most expensive class of defect in this codebase: implement what a tutorial exercises, run it, discover the
next missing piece from a wrong answer. The result passes the cases it was debugged against and makes **no
statement about anything else** — every branch no tutorial happened to reach is unmeasured, and unmeasured
here has repeatedly meant wrong.

The record is specific. `rhoSimpleFoam`'s `pEqn.H` has a transonic branch and a subsonic branch; its driver
picks `pcEqn.H` or `pEqn.H` on `simple.consistent()`; its `EEqn.H` uses a *different* kinetic-energy source
depending on whether the thermo's energy variable is `e` or `h`. Case-by-case porting reaches one arm of
each and leaves the others as plausible-looking code nobody has ever compared to OpenFOAM.

OpenFOAM's whole `rhoSimpleFoam` driver is **446 lines across 7 files**. Transcribing it systematically is
small. The closure *below* it — the thermo, the compressible turbulence set — is where the real surface is,
and that is exactly the part case-by-case porting leaves unmeasured.

## The rule

**Transcribe, don't paraphrase, and don't copy.**

A copy of a validated brae file cannot introduce a *re-interpretation*, which is why copying is tempting.
But it inherits every latent defect of the donor, and "correct for `simpleFoam`" is not "correct for
`rhoSimpleFoam`": the compressible paths carry `rho` weighting through `phiHbyA`, an energy equation with
its own boundary set, `rho.relax()`, and `divDevRhoReff` in place of `divDevReff`. A copy hides which of
those were considered. A transcription from the OpenFOAM file named in the manifest cannot.

So each component is written from its `of_file`, with that provenance in its header, and the manifest entry
is what says the component exists at all.

## Order of work, and it is not negotiable

1. **`_cpp` reference first** — host C++, one component at a time, each validated against OpenFOAM.
2. **The WHOLE case end-to-end in `_cpp`** before any `.cu` is written. A probe is not an end-to-end run.
3. **Then one CUDA module at a time**, testing the mixed build after each, with the `_cpp` path as the
   in-repo oracle — GPU against host, in-process, field by field.

Step 2 is the one that gets skipped under pressure, and skipping it is what forces every later defect to be
chased by instrumenting OpenFOAM instead of by asking the reference implementation sitting in the repo.

## What is authoritative

`manifest/rhoSimpleFoam.yaml` — one entry per component of the solver's closure, generated by
`tools/of_manifest.py rhoSimpleFoam`. A component that is not in the manifest does not get written; a
component whose `brae_status` is `TODO` has no implementation yet and the solver must **refuse** a case that
needs it, by name, rather than substitute something plausible.

## Relationship to what is already here

| path | status |
|---|---|
| `gpuRhoSimpleFoam.cu` | the working solver. **Untouched**, keeps its gates, stays the default until this path outruns it |
| `legacy/gpuRhoSimpleFoamLegacy.cu` | frozen; never deleted |
| `slice/` | an earlier re-port attempt whose stated policy was "Tier B — copied, then re-validated". **Superseded by this file**: copying is what we are not doing |
| `cpu/boundary_mu_eff.cuh` | predates this; not part of the ground-up port |

## Open findings, turned up by the port and belonging to other components

**`flowRateInletVelocity` disagrees with OpenFOAM by ~2.4e-01 on sbMatched.** Found while gating UEqn:
with the fixture's own inlet in place, `rAU` read 4.58e-05 and the inlet's `boundaryCoeffs` 4.15e-01
against OpenFOAM, while the outlet, the walls, and the inlet's `internalCoeffs` were all at machine
precision. Replacing only the inlet with a plain `fixedValue` — changing nothing else — took those to
6.12e-15 and 4.06e-15, which attributes the whole gap to the boundary condition and none of it to the
momentum assembly.

That BC derives the inlet velocity from `massFlowRate` and whichever `rho` it is handed, and it is the same
boundary condition behind the angledDuct defect (`flowRateInletVelocity` fed `thermo.rho()` while
`phiHbyA` used the relaxed solver `rho`). It is its own component and needs its own gate; it is recorded
here so the measurement is not lost, and `tests/rho_ueqn_vs_openfoam.sh` neutralises the inlet on purpose
so that the UEqn number stays attributable.

**brae has no energy boundary types (`basicThermo::heBoundaryTypes`).** OpenFOAM does not give `he` the
boundary conditions of `T`; it derives ENERGY ones from them — `fixedValue T` becomes `fixedEnergy`,
`zeroGradient T` becomes `gradientEnergy`, `inletOutlet T` becomes `mixedEnergy`. brae has no mapping, so
nothing today can construct `he`'s boundary field from a case.

This is not cosmetic. A `fixedValue` patch's `boundaryCoeffs` come from its **refValue**, so an `he` built
by borrowing `T`'s patch fields carries a *temperature* (1000 K) where the coefficients want an *energy*
(4.19e5 J/kg). Gating EEqn that way read a 19% source error that lived entirely on inlet-adjacent cells —
the interior was exact to 6e-13 — and it took a per-patch, then per-cell decomposition to attribute.

`tests/rho_eeqn_vs_openfoam.sh` rewrites the staged `he` into brae-known equivalents and **asserts the
rewrite is exact** (`gradientEnergy` only when its gradient is zero, `mixedEnergy` only when its
valueFraction is zero), so the substitution cannot silently stop being valid on another fixture. The real
mapping is its own component and still to be written.

**`fvMatrix::relax()` with a factor of 1 is not a no-op.** `relax(alpha)` runs a diagonal-dominance step
`D = max(|D|, sumMagOffDiag)` and adds `(D - D0)*psi` to the source *before* dividing by alpha, and
`fvMatrix::relax()` (no argument) is guarded by whether the case NAMES a factor — not by whether that
factor is below 1. Skipping relaxation at alpha >= 1 left the transonic pressure matrix wrong by 1.1e-07
on the diagonal and 4.4e-06 on the source.

It only showed on that branch, and that is the instructive part: the subsonic pressure equation is a
symmetric laplacian and already diagonally dominant, so for it `relax(1.0)` really is the identity. The
transonic branch adds `fvm::div(phid, p)`, which can break dominance in individual cells, and only there
does the step do anything. A gate covering one branch would have concluded the guard was fine.

**`inletOutlet::assignable()` was false in brae; OpenFOAM says true.** Found by the pcEqn gate.
`mixedFvPatchField::assignable()` is `false` (mixedFvPatchField.H:200) and brae inferred inletOutlet's
answer from that base — but `inletOutletFvPatchField` **overrides it back to `true`**
(inletOutletFvPatchField.H:163-164, *"True: this patch field is altered by assignment"*). `outletInlet`
derives from the same base and does **not** override, so its `false` was correct; the two differ on
purpose, and reading the base class instead of the derived one got exactly one of them wrong.

The consequence is in `constrainHbyA`, which replaces HbyA by U on every non-assignable patch: brae was
doing that at inletOutlet patches and OpenFOAM was not. Measured on pcEqn, HbyA's boundary was 1.3e-03 out
and the resulting phiHbyA 3.5e-03, while the internal field was exact to 1.4e-15 — a boundary-only error,
invisible in any interior comparison. This is **shared** code: `constrainHbyA` is also simpleFoam's
pEqn.H, so the fix is re-gated against the full tutorial set, not only the compressible ones.

**A fixture can make a component untestable, and say nothing about it.** sbMatched starts from a uniform
`p`, so `fvc::grad(p)` is analytically zero and *both* SIMPLEC corrections — the HbyA one and the snGrad
flux one — are no-ops at iteration 1. A gate taken there reports machine precision while testing none of
SIMPLEC; OpenFOAM's own HbyA correction moves the field by 9e-10 there, which is just `(1/V)*sum(Sf)`
failing to cancel at p ~ 1.1e5. `tests/rho_pceqn_vs_openfoam.sh` therefore develops the fields for 20
iterations and compares a restart, where the correction moves HbyA by 2.4e-01, and it **asserts** that
`rAtU` differs from `rAU` so the fixture cannot quietly go inert again.
