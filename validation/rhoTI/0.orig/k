FoamFile
{
    version 2.0;
    format ascii;
    class volScalarField;
    object k;
}

dimensions      [0 2 -2 0 0 0 0];
internalField   uniform 9.375;
boundaryField
{
    inlet    { type turbulentIntensityKineticEnergyInlet; intensity 0.05; value uniform 0.1; }
    outlet    { type zeroGradient; }
    hotWall   { type kqRWallFunction; value uniform 9.375; }
    coldWall  { type kqRWallFunction; value uniform 9.375; }
    frontBack { type empty; }
}
